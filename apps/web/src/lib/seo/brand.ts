/**
 * The brand blue as a hex value, for the places CSS tokens cannot reach: the
 * OG card, the browser's `theme-color`, and the web app manifest. It matches
 * `--brand-500` in `packages/ui/src/styles/tokens.css`.
 */
export const THEME_COLOR = "#001CD8";
